spring:

  jpa:
    show-sql: false

  datasource:
    hikari:
      maximum-pool-size: 15
      minimum-idle: 5

logging:

  level:

    root: INFO
    com.jmcavel.sigcav: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN